import 'package:flutter/widgets.dart';

class FinishWorkoutView extends StatelessWidget {
  const FinishWorkoutView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
